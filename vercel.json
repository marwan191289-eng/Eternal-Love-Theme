{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/wedding run build && rm -rf public && cp -r artifacts/wedding/dist public",
  "outputDirectory": "public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
